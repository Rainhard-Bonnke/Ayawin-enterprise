# Copy to .env.development.local (gitignored) for local overrides without changing production .env
NODE_ENV=development
ENABLE_DEMO_MODE=true
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
STRICT_PRODUCTION_CONFIG=false

# Keep your DATABASE_* and JWT_SECRET from .env
