/** Reserved for optional on-page notes; not used in the default UI. */
export function QuietNote(_props: {
  scenario: string;
  contextKey: string;
  context: unknown;
  className?: string;
}) {
  return null;
}
